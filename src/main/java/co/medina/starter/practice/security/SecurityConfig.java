// This security configuration was disabled because there's an existing SecurityConfig in package
// `co.medina.starter.practice.config`. Keep this file as a placeholder to avoid accidental re-adding
// another SecurityFilterChain. If you need to re-enable, restore the configuration there.

package co.medina.starter.practice.security;

// ...file intentionally left blank (disabled)
